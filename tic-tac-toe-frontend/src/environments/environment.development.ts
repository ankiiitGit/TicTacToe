// Development environment configuration (used during `ng serve`).
// The .NET backend is configured to listen on http://localhost:5000.
export const environment = {
  production: false,
  apiBaseUrl: 'http://localhost:5000/api',
};
